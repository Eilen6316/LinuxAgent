# network

LinuxAgent has no generic web-search promise. Network use depends on the configured provider, tools,
sandbox profile, and policy. It must not claim to browse or search the public internet unless an
enabled tool explicitly provides that capability.
