# config

Runtime configuration comes from `config.yaml`, including provider, model, sandbox settings, policy,
cluster, UI paths, and tool limits. `linuxagent check` validates configuration and reports runtime
diagnostics such as tool catalog metadata, sandbox runner boundaries, MCP, Skills, and monitoring.
