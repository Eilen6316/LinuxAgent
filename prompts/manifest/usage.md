# usage

LinuxAgent supports natural-language Linux operations requests, direct shell commands prefixed by
`!`, and slash commands such as `/help`, `/tools`, `/trace`, `/resume`, `/new`, `/clear`, `/exit`,
and `/quit`. Help text must describe the same command catalog used by completion.
