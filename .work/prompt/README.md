# .work/prompt/ · 预留目录

本目录仅为兼容早期文档而保留，**不作为运行时 Prompt 模板的真源**。

当前规则：

- 运行时 Prompt 模板统一放在仓库根 `prompts/`
- `.work/prompt/` 不存放运行时代码会读取的模板
- 如需调整 Prompt，修改 `prompts/`，并在 `.work/change/` 留痕

除非未来有新的架构变更记录明确启用本目录，否则保持最小化占位状态。
