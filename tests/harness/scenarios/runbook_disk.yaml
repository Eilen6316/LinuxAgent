scenario: "runbook disk usage"
provider_responses:
  - '{"goal":"investigate disk usage","commands":[{"command":"df -h","purpose":"Show filesystem usage","read_only":true,"target_hosts":[]}],"risk_summary":"runbook-guided read-only disk inspection","preflight_checks":["df -h"],"verification_commands":["df -h"],"rollback_commands":[],"requires_root":false,"expected_side_effects":[]}'
  - "Runbook disk analysis"
setup:
  runbooks_enabled: true
inputs:
  - role: human
    content: "机器磁盘满了，帮我看看哪里占空间"
expected_interrupts:
  - type: confirm_command
    matched_rule: LLM_FIRST_RUN
resume:
  decision: "yes"
  latency_ms: 90
expected:
  command_executed: true
  exit_code: 0
  response_contains:
    - "Runbook"
